{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**", "!.turbo/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "coverage/**"]
    }
  }
}